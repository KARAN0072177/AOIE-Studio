# AOIE Socket Server

This folder is reserved for the future real-time Socket.IO server.

Currently, AOIE uses the Next.js app inside `aoie-app` for frontend and backend logic through Next.js API routes.

The socket server will be added later only when real-time features are needed.